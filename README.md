# HTCondor Project

This project includes tools and scripts for analyzing HTCondor job logs, extracting features, and building job runtime prediction models.

## Structure

- `htcondor_eda/` ¡ª Scripts for log extraction, cleaning, and ProgramID construction.
- `model_training/` ¡ª Scripts for feature engineering and modeling.

> ?? This repository uses `.gitignore` to exclude all raw data, intermediate results, and model artifacts.
> Directory structure is preserved using `.gitkeep` and empty `README.md` files.
